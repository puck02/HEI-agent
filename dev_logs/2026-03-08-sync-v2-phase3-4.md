# 2026-03-08 Sync v2 实施记录（Phase 3.4 测试矩阵收口）

## 本阶段目标
- 补齐 Sync v2 核心协议自动化测试
- 固化删除同步与冲突处理关键行为

## 新增测试
- 文件：`tests/test_sync_v2_contract.py`

### 覆盖点
1. `sync_push` stale base 冲突短路
   - 用例：`test_sync_push_rejects_stale_base`
   - 断言：`conflicts=1`、`applied=0`、`status=conflict`

2. `sync_push` 结果聚合统计
   - 用例：`test_sync_push_aggregates_apply_results`
   - 断言：`accepted/applied/conflicts` 统计准确，`db.flush()` 正常执行

3. `sync_status` 能力声明
   - 用例：`test_sync_status_declares_tombstone_and_conflict_capabilities`
   - 断言：返回包含 `sync.tombstone.v1`、`sync.conflict.v1`

4. Tombstone payload 契约
   - 用例：`test_sync_tombstone_payload_roundtrip`
   - 断言：`SyncTombstone.payload` 字段可稳定序列化/读取

## 回归结果
- 执行命令：`/tmp/hei-agent-venv/bin/python -m pytest -q`
- 结果：`8 passed in 0.48s`

## 当前结论
- Sync v2 在后端侧已具备：
  - push/pull 主链路
  - tombstone 删除传播
  - 基础冲突拒绝策略（server-first）
  - 自动化回归保障（协议关键点）
