# 2026-03-08 Sync v2 实施记录（Phase 3.2）

## 本阶段目标
- 完善 `sync/pull` 的 health_entry 数据负载
- 确保 Android 回放可获得完整问答上下文（不仅是 entry 壳）

## 改造内容

### 后端 pull 负载增强
- 文件：`HEI-agent/app/api/v1/sync.py`
- 改造点：
  - `health_entry` pull 时新增查询并回传：
    - `question_responses[]`
    - `daily_advice`（若存在）
    - `daily_summary`（若存在）

## 验证结果
- 新建测试账号后执行：
  1. `POST /api/v1/sync/push`（包含 2 条 question_responses）
  2. `GET /api/v1/sync/pull?since=0&limit=20`
- 回归结果：
  - `health_entry` 记录成功返回
  - `question_responses` 数量正确（2 条）
  - question_id 列表正确（`q_sleep`, `q_mood`）

## 结论
- Android 端 `applyHealthEntry(...)` 现在能从 pull 结果中拿到完整问答数据并回放到 Room。
- 下一步可继续实现 tombstone 删除回放（双向删除一致性）。
